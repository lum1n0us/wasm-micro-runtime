{
	"name": "aee_host_agent",
	"version": "0.0.1",
	"description": "JLF AEE Host Agent",
	"vendor": "intel",
	"arch": "x86, x64, posix",
	"platform": "host",
	"type": "external",
	"category":  "app",

  "config": {
    "commands": [
          {
                "args": [
									"${OBD_CURRENT_MODULE_PATH}"
                ],
                "cmd": "${CMAKE_COMMAND}"
          }
    ]
  },

	"build": {
    "commands": [
            {
								"args": [
								],

								"cmd": "make"
            }
    ]
	},

	"target": "aee_host_agent",

	"dependencies": [
	]
}
